#include <bits/stdc++.h>
using namespace std;

#define ll long long
int main()
{
    int t;
    scanf("%d", &t);
    while (t--)
    {
        ll n, m;
        scanf("%lld %lld", &n, &m);
        for (int i = 0; i <= 64; i++)
        {
            
        }
        
    }
    
}