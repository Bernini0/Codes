#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    scanf("%d", &t);
    while (t--)
    {
        int n;
        scanf("%d", &n);
        double arr[n];
        for (int i = 0; i < n; i++)
        {
            scanf("%lf", &arr[i]);
        }
        sort(arr,arr+n);
        double a,b,c;
        double d = (-1);
        int k = 0;
        for (int i = 0; i < n-2; i++)
        {
            
        }
        
    }
    
}